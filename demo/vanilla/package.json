{
  "name": "demo-vanilla",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint ./src",
    "lint:fix": "eslint ./src --fix",
    "validate": "run-s lint build"
  },
  "dependencies": {
    "@yaasl/core": "workspace:*",
    "@yaasl/devtools": "workspace:*"
  },
  "devDependencies": {
    "vite": "8.0.16"
  }
}
